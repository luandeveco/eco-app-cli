import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
  LineContainer: {
    width: '100%',
    height: 40,
    top: 45,
    borderRadius: 8,
    backgroundColor: '#ffffff',
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: '0.9%'
  }
});